<% if !signed_in? %>
	<%= link_to("Sign In With Facebook", "/auth/facebook", "type" => "button", "class" => "dark button radius large", "style" => "width: 50%") %>
	<div>Don't worry, we are going to spam the shit out of you!</div>
<% else %>
	<div id = 'page'>Loading...</div>
	<script type = 'text/json' id = 'json'>
		{
			"current_user": <%= current_user.to_json %>,
			"users" : <%= User.all.to_json %>,
			"issues" : <%= Issue.all.to_json %>,
			"questions" : <%= Question.all.to_json %>,
			"challenges" : <%= Challenge.all.to_json %>,
			"tasks" : <%= Task.all.to_json %>
		}
	</script>

	<%= content_for :javascript do -%>
		<script type = 'text/javascript'>
			$(function() {
				var div, data;
				div = $('<div></div>');
				div.html($('#json').text());
				data = JSON.parse(div.text());
		
				QuizPop.initialize(data);
			});
		</script>
	<% end %>
<% end %>